import * as React from 'react';
import './players.css';
import './page.css';

export function Players(props: { handleError: (err: any) => void; }) {
    return (
        <main>
            <h2> Players</ h2>
        </main>
    );
}